---
import type { Reason } from '../../types/site';

interface Props {
  reason: Reason;
}

const { reason } = Astro.props;
---

<div class="reason" data-reveal>
  <h3>{reason.title}</h3>
  <p>{reason.text}</p>
</div>
