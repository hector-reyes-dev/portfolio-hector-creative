# Tasks — hero-name-image-hover

Hover letra→tile del nombre del Hero con variante aleatoria por activación, restauración demorada 180 ms, tiles solo con tokens existentes y nombre accesible intacto.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 766+ authored lines observed/planned (Slice A ~84 + Slice B ~682 + Slice C/D pending); exact diff-line count including deletions still pending |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (Unidad A: markup + SSR no-op) → PR 2 (Unidad B: controller + tests; ~682 authored lines, explicit size:exception accepted) → PR 3 (Unidad C+D: CSS + visual/browser validation; forecast pending) |
| Delivery strategy | ask-on-risk; human accepted size:exception for PR 2 only |
| Chain strategy | feature-branch-chain |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High
```

Riesgo High confirmado: Slice B requirió ~682 líneas authored para el controller y sus pruebas deterministas, por encima del presupuesto de 400. El humano eligió PRs encadenados con estrategia `feature-branch-chain` y aceptó explícitamente `size:exception` **solo para PR 2 / Unidad B**. No extender esta excepción a otras slices; PR 3 conserva el presupuesto estándar y requiere una nueva decisión si su forecast supera 400. El conteo exacto de diff lines (adiciones + eliminaciones) se medirá antes de delivery. Mantener PR 1 tracker → PR 2 → PR 3; commits, push y creación de PR siguen pendientes de autorización explícita.

## Unidad A — Contrato de markup y módulo SSR-safe (PR 1 candidata)

- [x] 1. Crear `src/lib/core/hero-name-hover.ts` exportando `initHeroNameHover(): () => void` como esqueleto no-op SSR-safe: nada accede a `document`/`window` al importar; sin `matchMedia` retorna un cleanup inocuo. Seguir el patrón de `src/lib/core/hero-emoji-burst.ts` (inicializador efímero, sin estado persistente). Verificar con `pnpm check`.
- [x] 2. Crear `src/features/hero-name-hover.test.ts` con el primer escenario de SSR-safety en entorno node (`@vitest-environment node`, patrón `src/features/projects/ssr-safety.test.ts`): importar el módulo, invocar `initHeroNameHover()` y su cleanup sin DOM no lanza. Verificar con `pnpm test -- src/features/hero-name-hover.test.ts`.
- [x] 3. Modificar `src/components/organisms/Hero.astro`: lista local estática y ordenada de 11 registros `{ slot, glyph }` (slots 00–10: H é c t o r R e y e s; slot estable de dos dígitos, no el glifo), seis primeros spans `.hero__name-letter` con `data-hero-name-letter` y `data-slot`, un espacio real emitido como texto (`{' '}`, no margen CSS ni `&nbsp;`), cinco spans restantes, y `<script>` procesado que importa e invoca `initHeroNameHover()` (patón de `HeroPortrait.astro`, sin modificar esa molecule). Verificar que el HTML compilado produce exactamente `Héctor Reyes` (nodos `childNodes`, no solo el fuente) ejecutando `pnpm test -- src/features/projects/navigation-semantics.test.ts` sin relajar aserciones. **Corrección posterior observada en navegador real (Orca, localhost:4321):** con spans inline-block, el navegador nombraba el h1 letra a letra (`H é c t o r R e y e s`); el h1 añade `aria-label` derivado de la misma lista de letras, byte a byte igual al texto visible (`Héctor Reyes`), sin `aria-labelledby` ni texto oculto: nombre accesible exacto sin duplicación (Label in Name satisfecho por coincidencia exacta). Ajuste verificado con los tests enfocados de las dos superficies y `pnpm check`; ver apply-progress. Rollback de la unidad: revertir los tres archivos; el nombre vuelve a ser texto plano.

## Unidad B — Controller completo con pruebas deterministas (PR 2 candidata, depende de A)

- [x] 4. Implementar en `src/lib/core/hero-name-hover.ts` el controller por raíz: estado con `WeakMap<HTMLElement, Controller>` (segunda llamada no agrega listeners, devuelve el mismo cleanup); raíz incompleta = no-op; buscar `[data-hero-name]` y sus once letras. En `pointerenter` válido (solo `event.pointerType === 'mouse'` + `'(hover: hover) and (pointer: fine)'` activo + `'(prefers-reduced-motion: reduce)'` inactivo): cancelar el timer de esa letra, crear perezosamente sus dos tiles (`<span class="hero__name-tile" data-variant="0|1" aria-hidden="true">` vacíos, `pointer-events: none`, `user-select: none`, hijos de la letra), sortear con `Math.floor(Math.random() * 2)` y fijar `data-active`/`data-active-variant` sin reemplazar el nodo de texto. En `pointerleave` de letra activa: un único `setTimeout(..., 180)`; al expirar retirar `data-active` (fade de vuelta 150 ms vía CSS). Reentrada antes de 180 ms: cancela el timer sin mostrar texto intermedio; solo cruza opacidades si el sorteo cambió. Handlers `change` de ambas `MediaQueryList`: cancelan timers, retiran `data-active`/selección y eliminan los tiles ya creados de inmediato; recuperar hover fino no autoactiva. Cleanup completo idempotente (timers, listeners de las once letras, media queries, tiles/atributos, WeakMap) que permite reinit posterior. Sin delegación global de `pointerenter`, intervalos ni dependencias externas. Verificar con `pnpm check` y `pnpm check:boundaries`.
- [x] 5. Ampliar `src/features/hero-name-hover.test.ts` con pruebas jsdom deterministas: fake timers y `matchMedia` stub; `Math.random` stub en 0 y valor cercano a 1 forzando ambas variantes en la misma letra con selección independiente por activación (sin falsear la independencia); init repetido = un listener por enter; tras la primera activación cada slot tiene dos tiles vacíos y `textContent` del `h1` sigue siendo exacto `Héctor Reyes`; timers 179/180 ms con restauración al expirar y cancelación con reentrada sin frame de texto; múltiples letras con timers independientes; reduce activo al cargar y cambio dinámico con tile visible (retiro inmediato) y recuperación sin autoactivar; pérdida de hover fino; `pointerType` touch/pen en híbrido; cleanup repetido y reinit sin listeners/tiles duplicados. Verificar con `pnpm test -- src/features/hero-name-hover.test.ts src/features/projects/navigation-semantics.test.ts src/features/projects/portfolio-consistency.test.ts` sin relajar aserciones existentes. Rollback de la unidad: revertir módulo y test a las verificaciones de la Unidad A.

## Unidad C — Estilos por tokens con salvaguardas (PR 3 candidata, depende de A y B)

- [x] 6. Añadir a `src/styles/portfolio/hero.css` el bloque del efecto: `.hero__name-letter { display: inline-block; position: relative; vertical-align: baseline }`, tiles absolutos `inset: 0` con caja/recorte locales sin padding ni ancho extra; en estado activo la letra conserva caja y texto pero su tinta pasa a transparente y el tile seleccionado cruza opacidad 0→1 (y el anterior 1→0) en 150 ms con `var(--ease-out)`; el delay de 180 ms vive en el JS, no como nueva duración de transición. Matriz explícita de 22 reglas `[data-slot="NN"] > [data-variant="0|1"]` fijando `--tile-surface`/`--tile-mark` exactamente según la tabla de `design.md` (solo `--accent`, `--accent-soft`, `--fill`, `--ink`, `--bg-soft`); variante 0 marca centrada redondeada y variante 1 franja diagonal recortada, radio `var(--r-sm)`, marca geométrica vía `::after { background: var(--tile-mark) }`, dimensiones internas en `em`/proporción limitadas por `inset`; sin sombras, gradientes, colores, radios ni duraciones nuevas. Salvaguardas CSS: bajo `(hover: none)`, `(pointer: coarse)` o `prefers-reduced-motion: reduce` las capas quedan invisibles y el texto al color heredado sin transición; nunca activar con `:hover`. Añadir comentario de cabecera con el contrato de imágenes futuras (`public/assets/hero-name/NN-V.webp`, URL `/assets/hero-name/NN-V.webp` (read-only), solo cambiar las 22 reglas visuales). Verificar con `pnpm build`.
- [x] 7. Comparar visualmente el nombre antes/después del split (kerning/tracking heredado, `letter-spacing` -.035em/-.03em, sin spacing artificial ni `white-space: nowrap` forzado; el espacio conserva el punto de salto) y revisar contraste marca/tile en tema claro y oscuro. Corregir solo si las medidas lo justifican, sin tocar tokens, `motion.css` ni `DESIGN.md`.

## Unidad D — Validación y evidencia de navegador (cierra la entrega)

- [ ] 8. Con `pnpm dev -- --host 127.0.0.1`, registrar la matriz de evidencia en `openspec/changes/hero-name-image-hover/evidence/`: desktop 1280 y 768 px con hover de las once letras (incluido `é` y ambas `e`) mostrando ambas variantes por letra en activaciones repetidas, salida con demora visible 180 ms y reentrada durante la demora sin parpadeo; bio/CTA/cajas estables y `scrollWidth <= clientWidth`; touch 390/320 px con taps/scroll sin tile ni letra pegada y nombre a 40 px; 200 % de zoom y copia exacta del nombre; árbol accesible con un único `h1`, nombre «Héctor Reyes», cero tiles anunciados y sin tab stops nuevos; reduce activo al cargar y cambio dinámico con tile visible; tema claro/oscuro y frontera 767/768 px; sin errores de consola.
- [x] 9. Ejecutar `pnpm agent:test` (equivale a `pnpm check && pnpm build`) y, por separado, `pnpm test` (Vitest); mantener ambos en verde, incluyendo en la suite los contratos de `navigation-semantics.test.ts` y `portfolio-consistency.test.ts` sin modificaciones a sus aserciones.
- [ ] 10. Antes de delivery, medir diff lines (`git diff --stat` contra la base), respetar PR1=A, PR2=B (size:exception explícitamente aceptada solo para esta slice) y PR3=C+D (sin excepción autorizada); si PR3 amenaza 400, pausar y pedir nueva decisión. Pedir autorización explícita antes de commits, push u abrir PRs.

## Notas

- **Corrección de nombre accesible (post tarea 6, observada en navegador real):** la suposición del diseño de que los spans inline aplanan a un único nombre accesible quedó refutada en el navegador integrado de Orca: el h1 se nombraba `H é c t o r R e y e s` aunque `textContent` fuera exacto. Sonda runtime-only con `aria-label` (sin tocar fuente) confirmó la dirección; la corrección de fuente (`aria-label` derivado de la lista de letras en `Hero.astro`) quedó cubierta por `navigation-semantics.test.ts` (página real renderizada: aria-label exacto, sin labelledby/tabindex/role/tiles servidos, un único espacio textual) y por el contrato explícito en `hero-name-hover.test.ts`. No se tocaron CSS/tokens: la corrección es de markup. La verificación de la matriz de navegador (tareas 7–10) sigue pendiente y no se marcó completa.
- Rollback global del cambio: restaurar el `h1` plano y retirar import/script, módulo, bloque CSS y test; verificar `pnpm agent:test` y un pase de hover con nombre estático.
- El escenario del spec «diez hovers» es comprobación observacional de navegador; el test determinista fuerza ambos resultados sin falsear la independencia del `Math.random`.
- Sin TDD estricto configurado: las pruebas deterministas de la Unidad B y la evidencia de la Unidad D son los checks prácticos de este pipeline.
- **Tarea 7 completada con medición real (registro honesto):** comparación antes/después del split medida en el navegador integrado de Orca a viewport real 856×1221 (NO a 1280/768/390/320): tracking heredado verificado (`letter-spacing: -1.96px` = −.035em × 56px), `line-height: 54.88px`, una sola línea renderizada (todas las letras comparten coordenada y), `white-space: normal` sin `nowrap` forzado y el espacio conservado como nodo de texto; medición parent de kerning texto plano 341.05px vs run partido ~343.6px (~2.6px, ~0.8%) dentro de la caja de 520px sin overflow — cambio modesto, **sin corrección CSS justificada**. Contraste marca/tile revisado en ambos temas: los 6 pares por tema pasan 4.5:1 (mínimo 4.86 claro / 7.40 oscuro), con spot-checks de primera persona coincidentes (11.87 oscuro, 14.82 claro). Sin tocar tokens, `motion.css` ni `DESIGN.md`.
- **Tarea 8 queda `- [ ]` (matriz registrada, mayoría no verificada):** existe `evidence/browser-validation.md` con la matriz verified/partial/not-verified, pero la mayoría de los ítems de la tarea (hover de las 11 letras, ambas variantes por letra, 1280/768/390/320, touch, 200% zoom, portapapeles, reduce al cargar/cambio dinámico, frontera 767/768, recorrido Tab real) no tienen evidencia observada: la interfaz documentada del navegador de Orca no expone viewport/emulación/preferencias y no se inventaron comandos. La tarea exige esa evidencia completa para marcarse.
- **Tarea 9 completada (verde observado):** `pnpm agent:test` → `pnpm check` exit 0 (0 errors, 0 warnings, 33 hints preexistentes) y `pnpm build` verde (`1 page(s) built`, `Complete!`); suite completa adicional `pnpm test` → 12 files / 135 tests passed, incluidos `navigation-semantics.test.ts` y `portfolio-consistency.test.ts` sin aserciones modificadas en esta sesión.
- **Tarea 10 queda `- [ ]`:** la medición exacta por slice requiere acciones de delivery (staging de hunks/commits por slice, y la base de la cadena sigue ambigua: `docs/design-md` hereda dos commits previos aceptados); se registró una medición conservadora read-only en apply-progress (~304 líneas medidas para PR3 C+D, bajo 400), pero la tarea exige conteo exacto pre-delivery y autorización humana; sin commits, push ni PRs.
- **Decisión humana de commit local (2026-09-23):** el usuario autorizó un único commit local agregado de los cambios actuales, anulando la separación de commits A/B/C+D para este commit solamente. No autoriza push ni PR ni una excepción de tamaño para una entrega futura. Las tareas 8 y 10 permanecen abiertas; la futura entrega y su base siguen sin resolverse.
