## 1. Composición y geometría del retrato

- [x] 1.1 Crear `src/components/molecules/HeroPortrait.astro` con botón nativo, nombre accesible, fotografía existente y capa decorativa hermana, e integrarla en `src/components/organisms/Hero.astro` mediante alias; verificar en navegador que imagen, contenido y CTA siguen disponibles, que el árbol accesible expone el control y que la UI no importa features.
- [x] 1.2 Adaptar `src/styles/portfolio/hero.css` para caja exterior 120 × 120 desde 768 px y 90 × 90 hasta 767 px, trasladando el margen al wrapper y conservando tokens, círculo, recorte, sombra y foco; medir `getBoundingClientRect()` en 320, 390, 767, 768 y 1280 px y comparar captura del Hero fuera de la foto.
- [x] 1.3 Establecer stacking local con foto delante de la capa absoluta no interactiva, sin overflow clipping del wrapper ni cambios globales; verificar que el foco no se recorta y las partículas no modifican geometría ni interceptan controles cuando se integre la emisión.

## 2. Emisión y controles

- [x] 2.1 Crear `src/lib/core/hero-emoji-burst.ts` e invocarlo desde el script procesado de la molecule, con consultas tipadas e inicialización idempotente; comprobar que montar/inicializar no emite, inicializar dos veces no duplica una activación y la ausencia del nodo no falla.
- [x] 2.2 Implementar una sola ruta de emisión para entrada hover de mouse, tap/click válido y activación nativa Enter/Espacio, filtrando eventos de compatibilidad y repetición de tecla según design; verificar entrada/permanencia/click/reentrada, dos taps, scroll cancelado, dispositivo híbrido y teclado con un conteo de emisiones observado en navegador.
- [x] 2.3 Implementar las 12 partículas mediante Web Animations y el array literal de 20 emojis completos de design, sorteando tamaño, trayectoria, rotación, duración y retraso por partícula; comprobar parámetros en varias emisiones y capturar fases inicial/intermedia/final que demuestren origen detrás de la imagen, crecimiento, ascenso y desaparición.
- [x] 2.4 Acotar trayectorias al viewport, sustituir la ráfaga previa al recibir otra acción y retirar nodos/referencias al finalizar o cancelar, sin ciclos recurrentes; verificar diez activaciones rápidas con máximo 12 partículas, ninguna a los 1.6 segundos de la última, sin scroll horizontal a 320 px durante el efecto ni errores de consola.
- [x] 2.5 Implementar reduced motion inicial y dinámico con cancelación/limpieza explícita y protección CSS de la capa; verificar hover/tap/teclado sin partículas bajo reduce, cambio durante animación y retorno a no-preference sin autoemisión. Comprobar además que sin JS o sin API de animación la foto, contenido y CTA siguen disponibles sin errores.

## 3. Aceptación funcional y cierre

- [x] 3.1 Registrar evidencia de navegador desktop 1280 × 900 y mobile touch 390 × 844, más 320 px y límites 767/768, con navegador, viewport, entrada, resultados y capturas secuenciales; cubrir dimensiones exactas, seis dimensiones aleatorias, conjunto de emojis, oclusión, una ráfaga por acción, ausencia de layout shift/overflow y conservación del Hero en claro/oscuro conforme a `design.md`.
- [x] 3.2 Registrar evidencia de Tab, foco visible, Enter, Espacio y tecla sostenida, activación accesible, capa ausente del árbol accesible y hit-testing transparente; añadir matriz desktop/mobile con reduce inicial y dinámico, ráfagas rápidas y degradación sin JS/API. Identificar expresamente emulación usada y cualquier escenario no ejecutado; no dar por cubierta tecnología asistiva solo con inspección del DOM.
- [x] 3.3 Tras cumplir los escenarios, enlazar evidencia funcional desde las tareas completadas y revisar que el diff se limita a los cuatro archivos de implementación previstos y artefactos/evidencia del change, sin dependencias, cambios de contenido ni modificaciones del tema; comprobar el alcance mediante revisión del diff antes de marcar esta tarea.
- [x] 3.4 Con aprobación humana vigente y las tareas anteriores respaldadas por evidencia, preparar el cierre y ejecutar `pnpm agent:verify hero-emoji-burst` (incluye `pnpm check` y `pnpm build`); registrar salida exitosa y recibo vigente junto a evidencia funcional. Como verify exige todas las casillas completas, marcar esta casilla inmediatamente antes de ejecutarlo y revertirla si falla; no declarar el cierre hasta observar éxito. No ejecutar commit, push, merge ni archive sin el encargo correspondiente.
